export const metadata = {
  title: "Groups",
  description: "Created by Manjesh",
};

export default function layout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (<div>{children}</div>);
}
