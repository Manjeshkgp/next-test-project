import { FC } from "react";
import GroupCotainer from "../components/GroupCotainer";

interface pageProps {}

const page: FC<pageProps> = ({}) => {
  return (<div>
  <GroupCotainer/>
  </div>);
};

export default page;
